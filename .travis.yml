matrix:
    include:
        - name: "Linux"
          services:
            - docker
          language: minimal
        - name: "macOS"
          os: osx
          language: shell
          compiler: gcc
        - name: "Windows"
          os: windows
          language: shell
          compiler: gcc
          before_install:
            - choco install python
            - choco install mingw
          env: PATH=/c/MinGW/bin:/c/Python37:/c/Python37/Scripts:$PATH

script:
    - ./scripts/ci.sh

deploy:
  - provider: script
    script: /bin/sh scripts/deploy.sh
    on:
      branch: master
      tags: true
